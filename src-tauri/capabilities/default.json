{
  "identifier": "default",
  "description": "Least-privilege capability for primary and spawned app windows (`main*`). Granted permissions are limited to: core IPC defaults for command invocation, dialog open/save for playlist and export pickers, event listen/unlisten for backend and menu events, start-dragging for the custom toolbar titlebar behavior, OS info for platform detection, native notification APIs, and liquid glass rendering.",
  "windows": ["main*", "settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:window:allow-create",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:webview:allow-create-webview-window",
    "core:window:allow-start-dragging",
    "core:window:allow-set-progress-bar",
    "os:default",
    "notification:default",
    "liquid-glass:default",
    "log:default"
  ]
}
